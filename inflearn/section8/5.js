function solution(arr) {
	function solution(n) {
		let arr = Array.from({ length: n + 1 }, () => 0);

		DFS(1);

		console.log(answer);
	}
}

let arr = [1, 3, 5, 6, 7, 10];
console.log(solution(arr));
